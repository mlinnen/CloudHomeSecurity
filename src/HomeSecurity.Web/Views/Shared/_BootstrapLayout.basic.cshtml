@using System.Web.Optimization
@using BootstrapSupport
@using NavigationRoutes
<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <title>@ViewBag.Title</title>
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="@Styles.Url("~/content/css")" rel="stylesheet"/>
        <link rel="stylesheet" type="text/css" href="~/Content/csharp.css" > 
        @RenderSection("head", required: false)
        @Html.Partial("_html5shiv")
        @* favicons and touch icons go here *@
    </head>
    <body>
        <div class="navbar navbar-inverse navbar-fixed-top">
            <div class="navbar-inner">
                <div class="container">
                    <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </a>
                    <a class="brand" href="#" title="change in _bootstrapLayout.basic.cshtml">My Security System</a>
                    <div class="nav-collapse collapse">
                        <ul class="nav pull-right">
                            @Html.Navigation()
                        </ul>
                    </div>
                </div>
            </div>
        </div>        
        <div class="container">
            @Html.Partial("_alerts")
            @Html.Partial("_validationSummary")
            @RenderBody()   
            <hr>
            <footer>
                <p>&copy; <a href="http://www.protosystem.net">Protosystem</a> @System.DateTime.Now.ToString("yyyy")</p>
            </footer> 
        </div>
        @Scripts.Render("~/bundles/jquery")
         @Scripts.Render("~/js")
        @Scripts.Render("~/Scripts/jquery.signalR-2.0.0.js")
        @Scripts.Render("~/signalr/hubs")
         @RenderSection("Scripts", required: false)
    </body>
</html>

